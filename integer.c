#include <stdio.h>
int main(){
    int a;
    int b;
    scanf("%d",&a);
    scanf("%d",&b);
    if ( a == b)
    {
        printf("both integers are equal");
    }
    else
    {
        printf("both integers are not equal");
    }
    
    
    return 0;
}